// tailwind.config.js (excerpt)
module.exports = {
  // ... other config
  theme: {
    extend: {
      colors: {
        primary: '#8B0000', // Horse blood red (dark red)
        'primary-dark': '#660000', // A darker shade for hover
        milk: '#FFFFFF', // Pure milk white (or use '#F5F5F5' for off-white if preferred)
      },
    },
  },
  // ... plugins, etc.
};